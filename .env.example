# ======== IMMICH-ADDON-SPACEY CONFIG (.env.example) ========

# 📁 Path to Immich's upload folder (MUST match the path used by Immich)
UPLOAD_LOCATION=/mnt/nfs/immich

# 🔐 Immich API key (get from Immich settings)
VITE_API_KEY=REPLACE_WITH_YOUR_IMMICH_API_KEY

# 🌐 Public URL where your Immich frontend is accessible (used for previews)
VITE_IMMICH_PUBLIC_URL=http://localhost:2283

# 🐘 Immich Postgres DB connection string
# Default uses Immich's internal container network
IMMICH_DB_URL=postgresql://postgres:postgres@immich_postgres:5432/immich

# 🛠️ Optional: Frontend dev server port
VITE_FRONTEND_PORT=5174

# 🔐 CORS: Comma-separated list of allowed frontend origins
ALLOWED_ORIGINS=http://localhost:5174,http://192.168.1.85:5174,http://immich-web:3000
